// let input;
// let total = 0;

// do {
//     input = prompt('введите число');
    
//         if (input === null) {
//         break;
//          }

//     input = Number(input);
//     total += input;
// } while (true);


// console.log(`Oбщая сумма ${total}`);

let input;
let numbers = [];
let total = 0;

const fn = function(...args) {
   let input = prompt('input the number');
   let numbers = numbers.push(input);
    if (input === null) {
        return;
    }
    else {
        for (number of numbers) {
         total += door;

        }
    }
    return total;
}

console.log(fn());





